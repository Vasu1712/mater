# --- Web / ingestion ---
# fastapi/starlette bumped so they're compatible with fastmcp 3.x's starlette>=1.0.
fastapi==0.137.1
uvicorn[standard]==0.49.0
pydantic==2.13.4
httpx==0.28.1

# --- Data stores ---
redis==8.0.0
asyncpg==0.31.0
qdrant-client==1.18.0

# --- Knowledge / embeddings ---
sentence-transformers==3.3.1

# --- MCP tool layer ---
# fastmcp 3.x and the agent's langchain-mcp-adapters both depend on the `mcp`
# SDK; pin the SDK explicitly so the tool server and agent share one version
# (mcp<1.27 lacks streamable_http_client, which adapters 0.3.0 imports).
fastmcp==3.4.2
mcp==1.27.2

# --- Agents (LangGraph + Ollama) ---
langgraph==1.2.5
langchain-core==1.4.7
langchain-ollama==1.1.0
langchain-mcp-adapters==0.3.0
